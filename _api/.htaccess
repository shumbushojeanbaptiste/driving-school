RewriteEngine On
RewriteBase /www.driving.rw/_api/

# Route everything to index.php if it's not a real file or folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
